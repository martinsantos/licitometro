# Preview for PR #PR_NUMBER
# Auto-generated - do not edit manually

upstream pr_PR_NUMBER_backend {
    server pr-PR_NUMBER-nginx:80;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;

    server_name pr-PR_NUMBER.dev.licitometro.ar;

    # Logs
    access_log /var/log/nginx/pr-PR_NUMBER-access.log;
    error_log /var/log/nginx/pr-PR_NUMBER-error.log;

    # Proxy settings
    location / {
        proxy_pass http://pr_PR_NUMBER_backend;
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # Buffering
        proxy_buffering off;
        proxy_request_buffering off;
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "PR #PR_NUMBER is healthy\n";
        add_header Content-Type text/plain;
    }
}
