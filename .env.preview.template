# MongoDB (Preview PR_NUMBER)
MONGO_USER=licitometro_preview
MONGO_PASSWORD=preview_pass_random_32
MONGO_URL=mongodb://licitometro_preview:preview_pass_random_32@pr-PR_NUMBER-mongodb:27017/licitaciones_preview_PR_NUMBER?authSource=admin
DB_NAME=licitaciones_preview_PR_NUMBER

# Authentication (same as production for convenience)
JWT_SECRET_KEY=CHANGE_ME_random_64_hex
AUTH_PASSWORD_HASH=CHANGE_ME_bcrypt_hash
TOKEN_EXPIRY_HOURS=24

# CORS (preview domain)
ALLOWED_ORIGINS=https://pr-PR_NUMBER.dev.licitometro.ar

# Storage (reduced for preview)
STORAGE_MAX_MB=100
RUN_HISTORY_KEEP=3
CACHE_TTL_HOURS=24
LOG_RETENTION_DAYS=2

# Notifications disabled for preview (avoid spam)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
NOTIFICATION_EMAIL_TO=
